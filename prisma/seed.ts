// Placeholder seed script. Populated in later phases.
async function main() {
  // eslint-disable-next-line no-console
  console.log('Seed: nothing to do in Phase 0.');
}

main().catch((err) => {
  // eslint-disable-next-line no-console
  console.error(err);
  process.exit(1);
});
